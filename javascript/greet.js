// Simple JavaScript Program to Greet the User
const userName = prompt("What is your name?"); // Ask for user's name

if (userName) {
  console.log(`Hello, ${userName}! Welcome to the site.`);
} else {
  console.log("Hello! Welcome to the site.");
}
